import { ContentContainer } from "@/styles";
import React from "react";

type Props = {};

const ContentCompoent = (props: Props) => {
  return <ContentContainer>ContentCompoent</ContentContainer>;
};

export default ContentCompoent;
